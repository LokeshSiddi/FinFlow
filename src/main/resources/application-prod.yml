spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/finflowdb
    username: postgres
    password: Siddi@0305
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true